version: "2"
sql:
  - engine: postgresql
    schema:
      - queries/schema.sql
    queries:
      - queries/systems.sql
    gen:
      go:
        package: db
        out: .
        sql_package: pgx/v5
