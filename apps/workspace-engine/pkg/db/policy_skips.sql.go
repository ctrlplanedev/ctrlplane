// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: policy_skips.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const deletePolicySkip = `-- name: DeletePolicySkip :exec
DELETE FROM policy_skip WHERE id = $1
`

func (q *Queries) DeletePolicySkip(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deletePolicySkip, id)
	return err
}

const getPolicySkipByID = `-- name: GetPolicySkipByID :one
SELECT id, created_at, created_by, environment_id, expires_at, reason, resource_id, rule_id, version_id
FROM policy_skip
WHERE id = $1
`

func (q *Queries) GetPolicySkipByID(ctx context.Context, id uuid.UUID) (PolicySkip, error) {
	row := q.db.QueryRow(ctx, getPolicySkipByID, id)
	var i PolicySkip
	err := row.Scan(
		&i.ID,
		&i.CreatedAt,
		&i.CreatedBy,
		&i.EnvironmentID,
		&i.ExpiresAt,
		&i.Reason,
		&i.ResourceID,
		&i.RuleID,
		&i.VersionID,
	)
	return i, err
}

const listPolicySkipsByVersionID = `-- name: ListPolicySkipsByVersionID :many
SELECT id, created_at, created_by, environment_id, expires_at, reason, resource_id, rule_id, version_id
FROM policy_skip
WHERE version_id = $1
  AND (expires_at IS NULL OR expires_at > NOW())
`

func (q *Queries) ListPolicySkipsByVersionID(ctx context.Context, versionID uuid.UUID) ([]PolicySkip, error) {
	rows, err := q.db.Query(ctx, listPolicySkipsByVersionID, versionID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []PolicySkip
	for rows.Next() {
		var i PolicySkip
		if err := rows.Scan(
			&i.ID,
			&i.CreatedAt,
			&i.CreatedBy,
			&i.EnvironmentID,
			&i.ExpiresAt,
			&i.Reason,
			&i.ResourceID,
			&i.RuleID,
			&i.VersionID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPolicySkipsByWorkspaceID = `-- name: ListPolicySkipsByWorkspaceID :many
SELECT ps.id, ps.created_at, ps.created_by, ps.environment_id, ps.expires_at, ps.reason, ps.resource_id, ps.rule_id, ps.version_id
FROM policy_skip ps
INNER JOIN deployment_version dv ON dv.id = ps.version_id
WHERE dv.workspace_id = $1
`

func (q *Queries) ListPolicySkipsByWorkspaceID(ctx context.Context, workspaceID uuid.UUID) ([]PolicySkip, error) {
	rows, err := q.db.Query(ctx, listPolicySkipsByWorkspaceID, workspaceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []PolicySkip
	for rows.Next() {
		var i PolicySkip
		if err := rows.Scan(
			&i.ID,
			&i.CreatedAt,
			&i.CreatedBy,
			&i.EnvironmentID,
			&i.ExpiresAt,
			&i.Reason,
			&i.ResourceID,
			&i.RuleID,
			&i.VersionID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertPolicySkip = `-- name: UpsertPolicySkip :exec
INSERT INTO policy_skip (id, created_at, created_by, environment_id, expires_at, reason, resource_id, rule_id, version_id)
VALUES ($1, COALESCE($9::timestamptz, NOW()), $2, $3, $4, $5, $6, $7, $8)
ON CONFLICT (id) DO UPDATE
SET created_by = EXCLUDED.created_by, environment_id = EXCLUDED.environment_id,
    expires_at = EXCLUDED.expires_at, reason = EXCLUDED.reason,
    resource_id = EXCLUDED.resource_id, rule_id = EXCLUDED.rule_id,
    version_id = EXCLUDED.version_id
`

type UpsertPolicySkipParams struct {
	ID            uuid.UUID
	CreatedBy     string
	EnvironmentID uuid.UUID
	ExpiresAt     pgtype.Timestamptz
	Reason        string
	ResourceID    uuid.UUID
	RuleID        uuid.UUID
	VersionID     uuid.UUID
	CreatedAt     pgtype.Timestamptz
}

func (q *Queries) UpsertPolicySkip(ctx context.Context, arg UpsertPolicySkipParams) error {
	_, err := q.db.Exec(ctx, upsertPolicySkip,
		arg.ID,
		arg.CreatedBy,
		arg.EnvironmentID,
		arg.ExpiresAt,
		arg.Reason,
		arg.ResourceID,
		arg.RuleID,
		arg.VersionID,
		arg.CreatedAt,
	)
	return err
}
