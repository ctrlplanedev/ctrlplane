{
  "name": "@ctrlplane/openapi-merge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "merge": "SKIP_ENV_VALIDATION=true tsx ./merge.ts"
  },
  "dependencies": {
    "glob": "^11.0.0",
    "openapi-merge": "^1.3.3",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@ctrlplane/eslint-config": "workspace:*",
    "@ctrlplane/tsconfig": "workspace:*",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:"
  },
  "prettier": "@ctrlplane/prettier-config"
}
