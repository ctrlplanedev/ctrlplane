apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ctrlplane.fullname" . }}-test-connection"
  labels:
    {{- include "ctrlplane.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ctrlplane.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
